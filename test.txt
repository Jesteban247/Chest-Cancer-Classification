to use it.
1. first run Template.py
    python Template.py
2. Update the setup.py and then the requirements.txt. After that
    pip install -r requirements.txt
3. Edit the init.py in the src folder
4. Create a file "common.py" in the path src/cnnClassifier/utils and edit it 
5. See some thiung in teh research fil the data ingestion
6. edit config/config.yaml for ingestion
7. make a litte change in params.yaml
8. check if some problem in kaggle
    pip uninstall kaggle -y
    pip install kaggle
Update config.yaml
Update params.yaml
Update the entity
Update the configuration manager in src config
Update the components
Update the pipeline
Update the main.py
Update the dvc.yaml
there is some _init_.py that you could delete (is for start the folders)
8. base model
Update config.yaml
Update params.yaml
Update the entity
Update the configuration manager in src config
Update the components
Update the pipeline
Update the main.py
Update the dvc.yaml
